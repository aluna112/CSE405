/*const postList = document.querySelector('.postss');

const setupPosts = (data) => {

  if (data.length) {
    let html = '';
    data.forEach(doc => {
      const post = doc.data();
      const li = `
        <button class="collapsible">${post.title}</button>
        <div class="content">
            <p>${post.content}</p>
        </div>
      `;
      html += li;
    });
    postList.innerHTML = html;
  } else {
    postList.innerHTML = '<h5 class="center-align">Login to view guides</h5>';
  }
  

};
*/
